# ATTESTATION.md (Phase 0)

Copy this file to your solution repository as `ATTESTATION.md` and fill it in
before starting Phase 1. Keep answers concise — a few sentences per prompt.

---

## Candidate

- Name: Hemant Singh
- Repository URL:
- Date: 24 June 2026

---

## Comprehension prompts

### 1. Fact-table grain

In one sentence, what is the grain of `reservations_hackathon`?

> Your answer: each row has the data for reservation x stay_date meaning there's one row for each night the reservation is for and no. of rooms doesn't affects the no. of rows.

### 2. Revenue columns

Name the two revenue columns and when to use each.

> Your answer: daily_room_revenue_before_tax and daily_total_revenue_before_tax. daily_room_revenue_before_tax should be used when the question is about just the room stay and not any extra facilities used by the client whereas daily_total_revenue_before_tax is all the revenue generated by that room so room revenue +  (package or breakfast)

### 3. Row vs reservation

Give one example question where counting rows would be wrong.

> Your answer: If user want to know the total number of reservations that we have because of the multiple rows created for each reservation depending on the stay nights.

### 4. Schema fields

Is there an `otel_challenge_token` column in the official schema? If so, what is it used for?

> Your answer: Can't find any column with this name.

### 5. Default OTB filters

Which `reservation_status` and `financial_status` values are excluded from default OTB?

> Your answer: "cancelled" is removed from "reservation_status" by default and "Provisional" is removed "financial_status" by default.

### 6. Stay date vs property date

When can `property_date` differ from `stay_date`, and which field drives monthly OTB?

> Your answer: both fields will generally hold same value but "property_date" may differ on night-boundary / audit rows. "stay_date" drives the monthly OTB.

### 7. Point-in-time OTB

How does `as_of_utc` change which cancelled rows are included in `get_as_of_otb`?

> Your answer: If there was any cancellation done after the specified "as_of_utc" then those reservations are considered as active because we are getting point-in-time OTB.

### 8. Block vs transient

How does `is_block` affect a “group vs transient mix” question?

> Your answer: if a question is asked about group then filter can be added on "is_block" as it value represents if a reservation is made for a group.

### 9. List pagination

How many reservations does the data site show per list page?

> Your answer: 100

### 10. Pagination completeness

How will you prove you did not miss the last list page during ETL?

> Your answer:

### 11. Tool grain

For `get_otb_summary`, what is the difference between `row_count` and `reservation_count`?

> Your answer: a reservation can have multiple rows depending on count of night stay has so row_count will be count of the all these rows but reservation_count will be count of just the rows that has unique "reservation_id".

### 12. Human-in-the-loop

Why must `get_as_of_otb` be gated behind approval, and what goes wrong if it is not?

> Your answer: There are changes that LLM might hallucinate in the parameters even after all the precautions and "get_as_of_otb" is a heavy operation so keeping HITL will help in checking if all the parameters are correct and is this what user is actually asking for.

### 13. Skill vs tool

Name one revenue-manager question that should load a **skill** but call **`get_segment_mix`**, not raw SQL.

> Your answer: 

---

## ETL design (one line)

Describe pagination strategy + idempotency approach + **anchor date** you will
scrape against (must match `/verify` on load day).

> Your answer: